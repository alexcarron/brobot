const GameStateService = require("./gameState.service");

describe('GameStateService', () => {
	let gameStateService;

	beforeEach(() => {
		gameStateService = new GameStateService(null, null, null);
	});
});